[project]
name = "fanout-child-workflows-python"
version = "0.1.0"
description = "Fan-Out with Child Workflows pattern demo (Python)"
requires-python = ">=3.12"
dependencies = [
    "temporalio==1.9.0",
]
