[project]
name = "eager-workflow-start-python"
version = "0.1.0"
description = "Eager Workflow Start latency optimization pattern demo (Python)"
requires-python = ">=3.12"
dependencies = [
    "temporalio==1.9.0",
]
